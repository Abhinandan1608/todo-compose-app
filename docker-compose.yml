services:

  app:
    build: .
    ports:
      - "3000:8000"

    environment:
      MONGO_URL: mongodb://mongodb:27017

    depends_on:
      - mongodb

  mongodb:
    image: mongo

    ports:
      - "27017:27017"